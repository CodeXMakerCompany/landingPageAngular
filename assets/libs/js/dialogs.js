$("button").click(function() {
  swal.fire("Success Message Title", "Well done, you pressed a button", "success")
});


function dialogo() {
    swal.fire("Success Message Title", "Well done, you pressed a button", "success")
}
